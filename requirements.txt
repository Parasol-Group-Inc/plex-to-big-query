# Plex → BigQuery ETL pipeline
# Pin versions for reproducible builds in Cloud Run

# ODBC connection to Plex ERP
pyodbc==5.1.0

# BigQuery client — write extracted data
google-cloud-bigquery==3.25.0

# Secret Manager — fetch ODBC credentials at runtime
google-cloud-secret-manager==2.20.0

# SendGrid — email report delivery
sendgrid==6.11.0

# DataFrame-based BQ writes and data wrangling
pandas==2.2.2

# BigQuery + pandas integration (to_gbq / load_table_from_dataframe)
db-dtypes==1.3.0

# GCS client — read report config YAML files at runtime
google-cloud-storage==2.18.2

# YAML parser — deserialize report config files
PyYAML==6.0.2